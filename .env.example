# Copy this to .env.local for local dev, and set the same vars in Vercel.
# Get these from Supabase: Project Settings -> API
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR-ANON-KEY

# Server-only (used by future Supabase admin scripts / cron). NEVER expose to client.
SUPABASE_SERVICE_ROLE_KEY=YOUR-SERVICE-ROLE-KEY

# Used for the auth redirect. Set to your prod URL in Vercel, e.g. https://bobo-golf.vercel.app
NEXT_PUBLIC_SITE_URL=http://localhost:3000
